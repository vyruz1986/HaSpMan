@using Web.Pages.Account
<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(Program).Assembly" PreferExactMatches="@true">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <NotAuthorized>
                    @if (context?.User?.Identity != null && !context.User.Identity.IsAuthenticated)
                    {
                        <RedirectToLogin />
                    }
                    else
                    {
                        <AccessDenied />
                    }
                </NotAuthorized>
            </AuthorizeRouteView>
        </Found>
        <NotFound>
            <LayoutView Layout="@typeof(MainLayout)">
                <div class="place-items-center expand d-grid">
                    <img class="mt-9 expand mw-9" src="images/404.jpg"
                         alt="Sorry, there's nothing at this address." />
                    <MudText Typo="Typo.body2">
                        <a href="https://www.freepik.com/vectors/disconnect">Disconnect vector created by jcomp - www.freepik.com</a>
                    </MudText>
                </div>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>